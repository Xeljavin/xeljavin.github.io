import { initTheme } from "./theme.js";
// later: import { initLang } from "./lang.js"

document.addEventListener("DOMContentLoaded", () => {
  initTheme();
  // initLang(); // when language system is ready
});

